import { LoginEmployeeModel } from './login-employee-model';

describe('LoginEmployeeModel', () => {
  it('should create an instance', () => {
    expect(new LoginEmployeeModel()).toBeTruthy();
  });
});
